package LiteratureComparison
  import Ionics annotation(
    uses(Ionics(version = "0.1.0"), Modelica(version = "4.0.0")));






  annotation(
    uses(Ionics(version = "0.1.0")));
end LiteratureComparison;